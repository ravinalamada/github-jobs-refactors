import React from 'react'

export default function SearchCity() {
  return (
    <Form>

    </Form>
  )
}

SearchCity.Container = function SearchCityContainer({children, restProps}) {
  <Container {...restProps}>{children}</Container>
}
